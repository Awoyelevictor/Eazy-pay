{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "User account information and balance",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string"
        },
        "email": {
          "type": "string"
        },
        "balance": {
          "type": "number",
          "description": "Wallet balance in NGN"
        },
        "phoneNumber": {
          "type": "string"
        }
      },
      "required": [
        "displayName",
        "email",
        "balance"
      ]
    },
    "Transaction": {
      "title": "Transaction",
      "description": "History of purchases or top-ups",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "airtime",
            "data",
            "funding",
            "electricity",
            "cable"
          ]
        },
        "amount": {
          "type": "number"
        },
        "service": {
          "type": "string",
          "description": "The specific bundle or service name (e.g. 1GB Data, DStv Compact)"
        },
        "network": {
          "type": "string",
          "description": "The network provider (MTN, Glo, etc.)"
        },
        "recipient": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "enum": [
            "success",
            "pending",
            "failed"
          ]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "aiGenerated": {
          "type": "boolean",
          "description": "Whether the transaction was initiated via the AI assistant"
        }
      },
      "required": [
        "type",
        "amount",
        "status",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User profile document. Status: Production v1.1.0 (Live Ready)"
    },
    "/users/{userId}/transactions/{transactionId}": {
      "schema": "Transaction",
      "description": "User transaction history"
    }
  }
}
